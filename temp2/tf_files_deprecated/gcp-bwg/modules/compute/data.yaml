---
data:
  - google_compute_subnetwork:
    subnetwork:
      project: ${var.project}
      region: ${var.region}
      self_link: ${var.subnetwork_name}
      name: ${var.subnetwork_name}
  - google_compute_zones:
    available:
      region: ${var.region}
      project: ${var.project}
      status: UP
