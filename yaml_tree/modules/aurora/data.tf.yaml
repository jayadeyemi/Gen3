data:
- aws_caller_identity:
    current: {}
- aws_vpcs:
    vpcs:
      tags:
        Name: ${var.vpc_name}
- aws_security_group:
    private:
      vpc_id: ${data.aws_vpc.the_vpc.id}
      name: local
- aws_vpc:
    the_vpc:
      id: ${data.aws_vpcs.vpcs.ids[0]}
