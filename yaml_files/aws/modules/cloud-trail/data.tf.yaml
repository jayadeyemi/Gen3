data:
- aws_iam_policy_document:
    trail_policy:
      statement:
      - effect: Allow
        actions:
        - logs:CreateLogStream
        - logs:PutLogEvents
        resources:
        - ${var.cloudwatchlogs_group}:*
