---
output:
  vpc_name:
    value: ${var.vpc_name}
  indexd_snapshot_id:
    value: ${aws_db_snapshot.indexd.db_instance_identifier}
  fence_snapshot_id:
    value: ${aws_db_snapshot.fence.db_instance_identifier}
  sheepdog_snapshot_id:
    value: ${aws_db_snapshot.sheepdog.db_instance_identifier}
