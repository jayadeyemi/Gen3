---
data:
  google_compute_network:
    network:
      project: ${var.project}
      name: ${var.network}
  google_compute_subnetwork:
    subnetwork:
      project: ${var.project}
      name: ${var.subnetwork}
